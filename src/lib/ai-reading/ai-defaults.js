/** Shared defaults safe for client + server imports (no env secrets). */

export const DEFAULT_READING_MODEL_KEY = 'gpt-oss:20b';
export const DEFAULT_READING_PROVIDER = 'llm7-free';
export const READING_AI_FEATURE = 'ai-reading';
